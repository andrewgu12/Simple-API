cd /vagrant
# Install all python packages
pip3 install -r requirements.txt

# Install gulp globally
npm install -g gulp-cli

# Install all npm packages
npm install

# Eventually start app, but not yet